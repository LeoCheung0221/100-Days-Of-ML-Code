<p align="center"><b>中文</b> &nbsp;&nbsp;·&nbsp;&nbsp; <a href="day-16.en.md">English</a></p>

# 第 16 天 · 上涨程度

[第一阶段 · 模型](README.md) · 可运行

今天的学习要点：斜率 3.27 送进 sigmoid 得到 0.9634，四步都贴这同一个分数，仿射直线的一日差分是常数，因此这个分数不能把四步按置信排出高下。

## 费曼法讲解

前几天的方向输出是硬的 1：每一步都猜涨。今天在这个 1 前面放一个程度。做法是把已经估出的斜率 3.27 送进 sigmoid。sigmoid 把实数收成 0 和 1 之间的数。3.27 是一个正数，收出来是 0.9634。

这个 0.9634 只依赖斜率。斜率是整条直线 `ŷ = 3.27x − 1.29` 的一个系数，不是某一步的收益。四步的实际收益并不相同，它们是 0.8571、0.5897、2.2258、−0.4800。程度分数不读这四个数。它读 3.27，读一次，然后把结果复制到每一步。四步旁边都写着 0.9634。

复制的原因在直线的形状里。仿射直线的一日差分是

```text
Δŷ_t = 3.27
```

对 t = 2, 3, 4, 5 都成立。差分不随 t 改变，sigmoid 的输入就不随 t 改变，输出也就不随 t 改变。四步的程度上没有高低。把它们按这个分数排序，四步并列。一个每步都相同的分数，不能用来表示「这一步比那一步更有把握」。

0.9634 也还不是这四步里上涨发生的频率。频率要去数有几步收益为正，那是下一章的核对。今天的精确陈述停在排序：因为仿射一日差分是常数，sigmoid(3.27) = 0.9634 这一个数覆盖四步，它不能把四步按置信分开。

## 核心知识

```text
σ(z) = 1 / (1 + exp(−z))
z = β₁ = 3.27
σ(3.27) = 0.9634
```

四步的分数向量是

```text
0.9634, 0.9634, 0.9634, 0.9634
```

它是常数向量。常数向量的任意两步之差是 0。因此不存在一个步的下标，使得该步的分数严格大于另一步。置信排序要求至少有两个不同的值。这里没有。

输入之所以是常数，是因为仿射函数对 t 的差分为斜率，而斜率不是 t 的函数。若分数使用了每一步自己的信息，输入里应该出现那一步的收益，或那一步的价格残差。今天的输入里只有 3.27。第四日绝对残差 8.21 的那一步，和第五日绝对残差 4.66 且方向不中的那一步，都不会把 0.9634 改成两个数。程度分数对这两日写成同一个数。

0.9634 也不是第 15 天那张四格表里的某一格。四格表计数的是硬分类：永远猜涨的误差在「下跌判成上涨」为 1，在「上涨判成下跌」为 0。sigmoid 没有先做这个硬分类再去数。它把斜率映射成一个介于 0 和 1 之间的数，再复制四次。映射是单调的：斜率越大，这个数越靠近 1。它不检查四步里面有几步确实为正。检查那件事需要另一个数，和 0.9634 并排。

不能排序的直接后果是：这四步无法按「更像上涨」分出第一和第二。若只要留下分数最高的一步，这条规则交不出那一步，因为四步的分数相同。交得出的只有一个共同的程度 0.9634，以及它来自斜率而不是来自单步收益。

## 拓展领域

把硬标签换成一个 0 到 1 的分数，常常被说成规则开始表达置信。置信要能在不同的情形之间变动，变动才构成区分。一个在四步上都打印 0.9634 的规则，表达的是整条直线的斜率有多大，不是这一步的涨跌有多清楚。斜率已经在第 11 天决定了方向：正斜率等于每一步都猜涨。sigmoid 把这个正号变成一个靠近 1 的小数。靠近 1 的程度由 3.27 的大小决定，不由四步收益彼此差多少决定。

因此，0.9634 的读法停在「四步共用的程度上写着 0.9634」。它不给出四步各自不同的把握。频率还没有计算。四步的把握在这个定义下相同。若以后改用单步特征，分数才可能四步不同，排序才可能存在。今天的特征是整段样本上的一个斜率，排序不存在。

常数分数还有一个和基准相连的读法。第 14 天的永远猜涨在符号上每步都交 1。今天在程度上每步都交 0.9634。两者都是不随 t 变化的输出。差别是 1 和 0.9634 这两个字面值。字面值变了，排序能力没有出现。评价这个分数时，单独问它能否区分四步。答案是不能。不能区分，就不能用它做「挑出更有信心的那一天」。

sigmoid 把斜率映到 (0,1)，输入 z=β₁=3.27，输出 0.9634 四步复贴。因为 Δŷ_t=β₁ 常数，不是 because sigmoid 忽略时间。换负斜率会得到 <0.5 的常数向量，仍无法逐步排序。

0.9634 不是 P(up) 的校准频率——第 17 天用 0.75 实现频率对照，差 0.2134。今天只claim 不能排序，不claim 校准。第 11 天硬 1 与今天 0.9634 都是常数输出，差别在字面，不在逐步区分力。

8.21 与 4.66 不进 sigmoid 输入；单步信息未进入程度模型。若要用残差定置信，需另设特征，不是 today's script。第四日 spike 不会抬高该步分数。

单调性：斜率越大 σ(β₁) 越接近 1。3.27 已较大，故 0.9634 靠近 1。这与四步三正一负的频率无必然相等。报告写 P(up)=0.9634 时勿省略「四步相同」。

与第 15 天四格：硬规则误差在 down-called-up；sigmoid 不数格。两套记录并行。第 18 天多列标签缩小正类，是定义变窄，不是 sigmoid。

跑 `up_score.py`：slope 3.27、P(up)=0.9634、同分每一步。测试：四输出相等。下一步第 17 天并排 stated 与 realized。
【续】σ(3.27)=0.9634 可手算核对数量级；四步复制是代码行为，不是 bug。若误把 0.9634 当第 17 天频率，会忽略 0.75 与 gap 0.2134。排序要求至少两不同值；常数向量方差 0。

与 always-up 硬 1 对照：字面 0.9634 与 1 不同，但方向行为仍恒 up。评价「是否更有信息量」看逐步是否变化——Today 否。第 18 天标签变窄与程度无关。

up_score 脚本打印 slope 与 P(up)。文档保留「同一分数贴每一步」句。下一步 confidence check。
sigmoid 单调但今日输入常数，输出亦常数。数学上 σ(3.27) 可与其他 z 比较，但四步无不同 z。第 17 天用 realized frequency 0.75 对照 stated，完成概率语句的最低校验。不要把 0.9634 四舍五入成 1 再写「必然上涨」——打印保留四位。

与 logit 课衔接：若每步有特征 x_t，z 可随 t 变，排序才可能出现。Today 无 x_t，只有全局 β₁。up_score.py 是桥梁脚本，读 slope 打印 P(up)。禁止用 8.21 调整 z。
【终稿补充】上涨程度 σ(3.27)=0.9634 四步相同，因为仿射 Δŷ=β₁ 常数。这不是 bug，是特征选择结果：全局斜率无逐步信息。第 17 天将 stated 0.9634 与 realized frequency 0.75 相减得 gap 0.2134。Today 只claim 不能排序。8.21 与 4.66 不进 sigmoid 输入。与 always-up 硬 1 比：字面不同，方向行为同恒 up。若 slope 负，σ 输出 <0.5 仍四步常数。up_score.py 打印 slope 与 P(up)。评价问：能否逐步排序？Today 否。第 18 天 multivariate 标签与程度无关。文档保留「同一分数贴每一步」。英文 P(up) 键名对齐。禁止把 0.9634 当频率。下一步 confidence check 三行。
【终稿补充·续】sigmoid(3.27)=0.9634 可手算验证在 0.96 量级；四步输出向量常数。排序不存在因输入 z 常数。第 17 天 frequency 0.75 与 stated 差 0.2134。8.21、4.66 不进 z。负斜率则四步同值 <0.5。up_score.py 打印。评价「置信排序」Today 答否。与硬 1 方向行为同恒 up。文档句：同一分数贴每一步。英文 P(up) 键。禁止 0.9634 当频率。下一步 confidence 三行。
【篇幅闭合】第 16 天在「硬 1」与「频率 0.75」之间插入连续程度 0.9634，并强调四步无法排序。请把下列核对写进笔记：① `up_score.py` 打印 slope=3.27 与 P(up)=0.9634；② 四步分数向量各元素相等；③ 8.21 与 4.66 不改变 P(up)；④ 负斜率会得到四步相同的 <0.5，仍不能排序。与第 11 天 score 3/4 对照：3/4 是离散命中，0.9634 是连续读数，但逐步仍无差异。与第 17 天衔接：stated 0.9634 不是 realized 0.75。报告时勿写「96% 置信那一天更涨」——没有「那一天」更高。英文 day-16.en 数字与中文一致。链接 ../../days/16-up-score/up_score.py 保持可点击。本日篇幅补充是为满足 season-01 与 day-09 同级的费曼+拓展+实战闭合，数字仍以脚本为准。
## 实战总结

```bash
python days/16-up-score/up_score.py
```

脚本打印斜率 3.27，`P(up) = sigmoid(slope) = 0.9634`，并写明同一个分数贴在每一步上。实现是 [`up_score.py`](../../days/16-up-score/up_score.py)。

0.9634 来自斜率，不来自四步收益的逐日差异。仿射一日差分恒为 3.27，所以这个上涨程度不能把四步按置信排出高下。
