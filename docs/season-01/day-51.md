<p align="center"><b>中文</b> &nbsp;&nbsp;·&nbsp;&nbsp; <a href="day-51.en.md">English</a></p>

# 第 51 天 · 五日收益的线性权重

[第一阶段 · 模型](README.md) · 可运行

今天的学习要点：在 AAA 上，用 lag1–lag5 简单收益预测当日简单收益。OLS 得 lag1=−0.1359，lag2=0.0829，lag3=0.1094，lag4=−0.1726，lag5=−0.0803，截距=0.0023。min=−0.1726，max=0.1094，test MSE=0.000081。五系数同量级，非等权。

## 费曼法讲解

第 50 天还在价格水平上做方向投票；从今天起特征链改成「过去五日简单收益 → 当日简单收益」。把一行想成卡片：左五格是 lag1 到 lag5，右格是当日要解释的收益。训练段取时间顺序前百分之七十五，在这叠卡片上找五个权重加一个截距，使训练平方误差尽量小。

五个权重不是「各 0.2」。lag4=−0.1726 绝对值最大；lag3=0.1094 是最大正数；lag1=−0.1359 也是显著负权重。lag2=0.0829、lag5=−0.0803 较小，但仍与 lag1、lag3 同量级。截距 0.0023 是整堆卡片上的常数项，不是第六个 lag。

测试 MSE 0.000081 只在后十九行上计算：用训练段 frozen 的六个数去预测，再与标签比平方后平均。min 与 max 相差约 0.28，等权假设不成立。权重正负交替，说明直线在做加权求和，而不是只盯 lag1。

手算核对时，请把 lag4 与 lag3 放在同一张小表里：一个拉低预测、一个抬高，合起来不是「最近一天说了算」。若你逐行算 (y−ŷ)² 再平均，得到 0.000081 才与脚本一致；用训练段重估系数再去测十九行，数字会漂，那是切分纪律错误。

数据来自 days/data/panel.csv 的 AAA 行：简单收益由复权收盘相邻两日比值减一。有效样本从第五个收益之后才开始，因此比原始行数少五行。默认切分是这些有效行按日期排序后的前百分之七十五训练、其余测试（本段多数课为 train=54、test=19）。同一交易日的 high、low、close 不能解释当日收益；同日 market 收益也不能当作合法标签或特征，除非当天脚本明确允许。

## 核心知识

```text
weight lag 1 = -0.1359
weight lag 2 = 0.0829
weight lag 3 = 0.1094
weight lag 4 = -0.1726
weight lag 5 = -0.0803
intercept = 0.0023
weight min = -0.1726  weight max = 0.1094
test MSE = 0.000081
```


return 上的 test MSE 与早期「时间对价格水平」的 SSE 不是一列数；第 51 天及以后不要把第 45、46 天的树 SSE 贴进 return 表。hold-out 行是唯一评分集合；系数与阈值只在训练段估计。

| 概念 | 本课是否变动 | 备注 |
|---|---|---|
| 五 lag 直线系数 | 多数课 frozen | 来自第 51 天 train |
| test MSE 0.000081 | 仅 MSE 课重印 | 诊断课改读 MAE/方向/账单 |
| forbidden OHLC/market | 合同不变 | 见第 56–57、67 天 |
| train/test 行数 | 默认 54/19 | 第 58 天按年切分例外 |

## 核心块逐行读法

第 51 天 stdout 核心块共 8 行。下面逐行说明读法纪律（不是改写成口语数字）：

- `weight lag 1 = -0.1359`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。
- `weight lag 2 = 0.0829`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。
- `weight lag 3 = 0.1094`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。
- `weight lag 4 = -0.1726`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。
- `weight lag 5 = -0.0803`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。
- `intercept = 0.0023`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。
- `weight min = -0.1726  weight max = 0.1094`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。
- `test MSE = 0.000081`：键名、等号两侧空格、负号与小数位须与终端一致。中文解释可以写长，但这行英文与数字是 diff 基准；不要把 false 写成中文「否」、不要把 FORBIDDEN 行删掉、不要把 bill −18 写成 18。若该行含 test MSE，默认指 hold-out 平均平方误差；若含 FORBIDDEN，表示该列不得进入合法特征。批改时对此行做逐字 diff，而不是只看摘要段。

lag-5 阶段常用锚点：line test MSE 0.000081（第 51、56、57、69 等课）、volume helped on the test stretch = false（第 54 天）、total bill line = -18.0000（第 75、79 天）。若本课核心块不含某锚点，正文中也不要为了「看起来完整」而提前写入；若本课含某锚点，不得四舍五入或去掉负号。第 58 天按年切分时的 0.000782 是切分实验，不能覆盖 0.000081 标尺。第 65、68 天 BBB 的 0.000105 与 AAA 并列，禁止自动搬运结论。

## 拓展领域

矩阵写法：设计矩阵每行是 [lag1,…,lag5,1]，响应是当日收益。正规方程在训练行上求 β̂；测试行只代入 ŷ=Xβ̂。这与第 9 天行序不变性兼容：打乱训练行顺序不改变 OLS 解，但打乱时间会破坏 lag 特征构造——本课 lag 已按日历对齐，不要 shuffle。

第 52 天会在同一表上放一棵树桩；0.000081 是直线在后段的标尺，不是方向命中率。研究日志里把 forbidden 规则、切分与 test MSE 同页保存；英文 stdout 是权威来源，中文是解释层，六位小数必须一致。

BBB 与其他 name 只在指定天出现；不要把 AAA 的 0.000081 自动搬到别的股票。

## 与前后课的关系

往回看，第 50 天还在三条价格水平拟合序列上比一步方向；今天第一次把设计矩阵写成五列滞后简单收益加截距，标签是当日简单收益。这是整个 lag-5 合同的原点：以后凡是出现 test MSE 0.000081，都是在这套系数与 nineteen 行 hold-out 上复算。向前看，第 52 天会在同一表上放单 lag 树桩；请先把五个权重记牢，尤其 lag4=−0.1726 与 lag3=0.1094 的对抗关系。写笔记时画一条时间轴：左端是价格课，右端是 return 课，第 51 天是交界点。任何把 3.2700 斜率或 96.339 RSS 写进本页的人，需要回到第 1–10 天复习标签定义。团队分工建议：一人跑脚本抄 stdout，一人用表格解释权重符号，第三人只负责核对 train fifty-four 与 test nineteen 是否出现在打印里（本脚本隐式使用默认切分，但第 56 天会显式打印行数）。

给工程师的阅读顺序：先跑本日脚本对照 stdout，再读正文；不要跳过第 51 天直接读诊断课，否则不知道直线系数从哪来。写单元测试时，对 frozen 系数在 hold-out 上断言 MSE 或账单与打印一致；失败常见原因是混用 train 行或把 BBB 行掺进 AAA。文档截图应至少露出核心块英文键名与六位小数，便于他人 diff。复现环境建议 python3 与仓库 pinned numpy；末位浮点差不改变本课结论，但不应改合同整数如 quiet=10、jump=5、direction wrong=3。

## 归档与交付（第 51 天）

交付最小包：本课核心块英文、今天的学习要点、以及你亲自跑出的终端 diff。第 51 天在 season 第一阶段中的位置，应能在十行 recap（第 70 天）里找到对应句子或能解释为何本日更细。归档时把切分方式、name 列、标签定义与分数绑在同一文件名；不要用「最终 MSE」概括不同实验。同事若只复制中文摘要，请要求对方补 stdout；否则 lag-5 锚点 0.000081、volume helped=false、bill −18 可能在传播中丢失负号或 false。下一阶段若改任务，请新建 day 编号或 season，而不是覆盖本文件核心块。

## 复跑与 diff

在仓库根目录执行 `python3 days/51-five-lag-weights/five_lag_weights.py`，把终端输出保存为文本，与本文核心块逐行 diff。第 51 天只接受 stdout 与核心块一致；若你本地 panel 被改，应另存实验记录而不是覆盖本页字面量。diff 时忽略终端前缀路径与警告行，但不忽略键名大小写、空格与 false 小写。若使用 IDE 运行配置，确认 working directory 指向仓库根，否则 panel.csv 相对路径会失败。建议把 diff 结果贴进 PR 描述：「stdout 与 day-XX 核心块一致 / 不一致（附原因）」。

## 今日要点展开

文首学习要点：在 AAA 上，用 lag1–lag5 简单收益预测当日简单收益。OLS 得 lag1=−0.1359，lag2=0.0829，lag3=0.1094，lag4=−0.1726，lag5=−0.0803，截距=0.0023。min=−0.1726，max=0.1094，test MSE=0.000081。五系数同量级，非等权。。第 51 天笔记展开时，应把上述句子拆成「对象（AAA/BBB/面板行）」「标签（return/excess）」「切分（54/19 或 33/40）」「分数（MSE/MAE/bill/计数）」四格表；缺一格就不要发出结论。数字一律来自核心块，不要用四舍五入口语替代六位小数。

## 常见误读（第 51 天）

- 误读：只看中文摘要、不 diff stdout，导致 MSE 0.000081 或 bill −18 符号丢失。
- 误读：把 train 行分数当作泛化证据；本季只在 hold-out 上交付 test MSE、MAE 与 bill。
- 误读：在脚本未打印的日子写入 volume helped=false 或 total bill line=−18.0000。
- 误读：把第 58 天 0.000782 与 lag-5 标尺 0.000081 混成「模型变差」而不提切分。
- 误读：把 BBB 0.000105 当成 AAA 的结论；第 65、68 天要求并列 name 行。
- 误读：把价格水平 SSE 贴进 return 表；第 45、46 天与第 51 天以后不是一列数。


## 交作业格式（第 51 天）

1. 粘贴核心块（英文键名与数字与终端一致）。
2. 粘贴今天的学习要点（中文可写，但数字与核心块一致）。
3. 附 `python3 days/51-…` 的运行截图或日志文件路径。
4. 用三句话说明本课与第 51 天 lag-5 直线或第 70 天十行 recap 的关系。
5. 若本课出现 FORBIDDEN、false、not a result，必须在提交中保留英文原句。
6. 不要附加第 45、46 天价格 SSE 或未经脚本打印的 bill/MSE 锚点。


## 实战总结

```bash
python3 days/51-five-lag-weights/five_lag_weights.py
```

脚本应打印与核心块一致的 stdout 行。实现是 [`five_lag_weights.py`](../../days/51-five-lag-weights/five_lag_weights.py).

今天交出去的是五个 lag 权重与 test MSE 0.000081。下一课在同一滞后收益上切一棵树桩。

自检清单：训练/测试行数是否与脚本一致；核心块英文键名、符号、六位小数是否与终端逐字相同；FORBIDDEN 与 not a result 句是否原样保留；不要把 line 与 tree 的 MSE 或 bill 列对调；AAA 的 0.000081 与 volume helped=false 与 bill −18 等 lag-5 锚点未被改写。

## 自测问答

1. 本课 stdout 核心块首行是什么？应能逐字写出 `weight lag 1 = -0.1359`。
2. 今天的学习要点是否覆盖核心块每一类信息？若遗漏 FORBIDDEN 或 bill 规则，请回到脚本补读。
3. 若把 train 行混入 test 评分，MSE、MAE 或 bill 会如何失真？本课正确做法是只在 hold-out 上读分。
4. lag-5 锚点中，哪几个出现在本课？未出现的锚点不要写进本页「总结」以免污染合同。
5. 第 51 天与第 51 天的衔接点是什么？用一句话说明变量是否 frozen、切分是否改变、标签是否改变。
6. 英文 miss mode / FORBIDDEN / not a result 句出现时，为什么必须保留英文？因为批改 diff 以终端为准，中文同义改写会造成假一致。

